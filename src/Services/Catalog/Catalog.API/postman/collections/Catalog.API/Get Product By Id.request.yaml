# yaml-language-server: $schema=../../postman-schema.json
$kind: http-request
name: Get Product By Id
method: GET
url: '{{baseUrl}}/products/{{productId}}'
order: 2000
description: Retrieves a single product by its GUID ID.
